# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge to Develop
"on":
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install & Build
        run: yarn unprepare && yarn && yarn nx run frontend:build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_HARPER_MEDIA_87677 }}"
          channelId: live
          projectId: harper-media-87677
          entryPoint: ./packages/frontend
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
